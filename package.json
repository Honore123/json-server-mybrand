{
  "name": "my_brand_json_server",
  "version": "1.0.0",
  "description": "rest api for testing ",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
